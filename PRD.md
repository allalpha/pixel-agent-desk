# 📋 PRD: Pixel Agent Desk v2.0

> **최종 통합 PRD**: 본 문서는 모든 전문가 팀의 분석 결과를 통합한 종합적인 제품 요구사항 문서입니다.
> **상세 문서**: 완전한 통합 PRD는 `PRD_FINAL.md`를 참조하세요.

## 비전 및 미션

### 비전 (Vision)
"개발자가 AI 에이전트와 함께 작업하는 방식을 시각적으로 혁신하여, AI 활용 개발의 생산성과 즐거움을 극대화한다."

### 미션 (Mission)
"Claude CLI 사용자가 터미널의 제약을 넘어 AI 에이전트들의 활동을 직관적으로 이해하고 관리할 수 있는 최고의 시각화 플랫폼을 제공한다."

### 핵심 가치 (Core Values)
- **투명성 (Transparency):** AI 에이전트의 모든 활동을 실시간으로 시각화
- **즉각성 (Immediacy):** 50ms 미만의 반응속도로 즉각적인 피드백 제공
- **신뢰성 (Reliability):** PID 기반 생명주기 관리로 99.9% 업타임 보장
- **즐거움 (Delight):** 픽셀 아트와 캐릭터 인터랙션으로 즐거운 사용 경험 제공

## 목표
Claude CLI 사용 중인 세션을 픽셀 캐릭터로 시각화하고 세션의 생명주기(시작/종료)를 안정적으로 관리

## 핵심 기능
1. **Hook 기반 실시간 이벤트 수신**: Claude CLI의 Hook 시스템을 통해 모든 이벤트를 실시간 수신 (14개 이벤트 타입 지원)
2. **에이전트 타입 시각화**: Main(기본), Sub(서브에이전트), Team(에이전트 팀)을 배출색과 태그로 명확히 구분
3. **상태 및 프로젝트 시각화**: Working/Done/Waiting 상태 애니메이션 + 에어로젝트별 자동 그룹화(정렬)
4. **정교한 프로세스 관리**: PID 기반 3초 주기 생사 확인 및 앱 시작 시 100% 세션 복구
5. **인터랙티브 터미널 포커스**: 캐릭터 클릭 시 해당 Claude 세션이 실행 중인 터미널 창을 OS 레벨에서 포커스

## 상태 정의
| 상태 | 조건 | 애니메이션 | 말풍선 색상 |
|------|------|-----------|------------|
| Waiting | 세션 시작 / 사용자 입력 대기 상태 | 앉아 있는 포즈 (frame 32) | 회색 (#9e9e9e) |
| Working | `UserPromptSubmit` / `PreToolUse` 이벤트 | 일하는 포즈 (frames 1-4) | 주황색 (#ff9800) |
| Thinking | 복잡한 추론 중 | 생각하는 포즈 | 파란색 (#2196f3) |
| Done | `Stop` / `TaskCompleted` / 2.5초 Idle 감지 | 춤추는 포즈 (frames 20-27) | 녹색 (#4caf50) |
| Help | `PermissionRequest` / `Notification` 등 상호작용 필요 | 도움 요청 포즈 | 파란색 (#2196f3) |
| Error | `PostToolUseFailure` 이벤트 | 경고 포즈 (frames 0, 31 Blink) | 빨간색 (#f44336) |
| Offline | 프로세스 종료 감지 | 흑백 처리 | 짙은 빨강 (#ef5350) |

## 에이전트 생명주기

### 이벤트 기반 상태 전환
1. **SessionStart**: 새 에이전트 생성 + `Waiting` 상태
2. **UserPromptSubmit**: 사용자가 입력을 제출하는 즉시 `Working` 상태로 전환
3. **Stop / TaskCompleted**: Claude 응답 완료 시 즉시 `Done` 상태로 전환
4. **PostToolUse + 2.5s Timer**: 응답 완료 훅 누락 시 2.5초 뒤 자동 `Done` 전환
5. **PermissionRequest / Notification**: 사용자의 선택이나 확인이 필요한 `Help` 상태
6. **SessionEnd / Process Dead**: 세션 종료 혹은 프로세스 종료 감지 시 에이전트 제거

### 초기화 탐색 자동 무시
- 첫 `PreToolUse` 이벤트는 세션 초기화(cwd 탐색 등)로 간주하여 무시
- 두 번째부터 사용자 요청에 의한 실제 도구 사용으로 처리

### 정교한 프로세스 관리
1. **PID 기반 실시간 감시**: `process.kill(pid, 0)`을 통해 3초마다 프로세스 생존 확인
2. **부활 시스템 (Recovery)**: 앱 시작 시 살아있는 Claude PID를 조회하여 기존 세션 즉시 복구
3. **터미널 포커싱**: 캐릭터 클릭 시 해당 Claude 세션이 실행 중인 터미널 창을 최상단으로 포커스

## 아키텍처
```
┌─────────────────────────────────────────────┐
│              Claude CLI                     │
│  ┌───────────────────────────────────────┐  │
│  │         Hook Events (All Types)       │  │
│  └───────────────────┬───────────────────┘  │
└──────────────────────┼──────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────┐
│               hook.js                       │
│    (stdin → HTTP POST localhost:47821)      │
└──────────────────────┼──────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────┐
│    main.js (HTTP Hook Server + IPC)         │
│  ┌──────────────┐  ┌──────────────────┐    │
│  │ AgentManager │◄─┤  Event Handlers  │    │
│  └──────┬───────┘  └──────────────────┘    │
│         │                                  │
│         └──────────────┐                   │
│                        ▼                   │
│                   [IPC Bridge]              │
└────────────────────────┼───────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────┐
│            renderer.js / UI                 │
│  ┌──────────────┐  ┌──────────────┐        │
│  │ Multi-Agent  │  │ 0-Agent Idle │        │
│  │ (Cards Grid) │  │ (Wait Pose)  │        │
│  └──────────────┘  └──────────────┘        │
└─────────────────────────────────────────────┘
```

## 파일 구조
- `main.js`: Electron 메인 프로세스, HTTP 훅 서버(Port 47821), 자동 훅 등록
- `hook.js`: 범용 훅 스크립트 (Claude CLI → HTTP 서버)
- `sessionend_hook.js`: 세션 종료 시 JSONL에 SessionEnd 기록
- `agentManager.js`: 에이전트 객체 관리 및 상태 변경 이벤트 발행
- `renderer.js`: UI 렌더링 및 애니메이션
- `preload.js`: IPC 통신 브릿지
- `utils.js`: 유틸리티 함수

## 구현 현황
- ✅ 실시간 훅 서버 (TeammateIdle, Subagent 등 전체 지원)
- ✅ PID 기반 정교한 생사 확인 (3초 간격)
- ✅ 앱 시작 시 기존 세션 자동 복구 (Real PID 매칭)
- ✅ 터미널 창 포커싱 (캐릭터 클릭 시 팝업)
- ✅ 에이전트 타입 구분 (Main: 핑크/Sub: 보라/Team: 파랑)
- ✅ 프로젝트별 자동 그룹화 및 정렬
- ✅ 자동 훅 등록 및 설정 관리

## 향후 과제 및 로드맵

### 단기 개선사항 (1-3개월)
1. **사용성 개선**
   - 에이전트 네이밍 개선 (사용자 직접 이름 지정)
   - 상태 메시지 구체화
   - 키보드 단축키 추가

2. **안정성 강화**
   - 에러 핸들링 강화
   - 로그 및 디버깅 개선
   - 테스트 커버리지 50% 달성

3. **Mission Control 완성**
   - 실시간 에이전트 상태 동기화
   - 작업 히스토리 타임라인
   - 통계 및 차트

### 중장기 기획 (3-12개월)
1. **플랫폼 확장**
   - macOS 지원 (Cocoa API)
   - Linux 지원 (X11/Wayland)
   - 통합 설치 프로그램

2. **기능 확장**
   - 작업 관리 (예약, 배치 처리)
   - 협업 기능 (팀원 상태 공유)
   - 분석 및 인사이트 (생산성 지표)

3. **생태계 구축**
   - 플러그인 시스템
   - 테마 및 커스터마이징
   - 통합 및 연동 (GitHub, Slack)

### 기술 부채 관리
| 우선순위 | 항목 | 예상 시간 |
|----------|------|----------|
| 높음 | 테스트 커버리지 0% → 80% | 40시간 |
| 높음 | 메모리 누수 수정 | 8시간 |
| 중간 | OS 호환성 (macOS/Linux) | 20시간 |
| 중간 | 코드 중복 제거 | 16시간 |
| 낮음 | 문서화 강화 | 16시간 |

## 성공 지표

### 제품 지표
- **MAU (1개월):** 100명 → **MAU (12개월):** 5,000명
- **리텐션율:** 주간 40%, 월간 30%
- **평균 세션 시간:** 30분 이상
- **NPS:** 50점 이상

### 기술 지표
- **앱 시작 시간:** < 1초
- **이벤트 수신 지연:** < 50ms
- **복구 성공률:** 95% 이상
- **테스트 커버리지:** 80% (12개월)

## 실행 방법
```bash
# 1. 의존성 설치
npm install

# 2. 앱 실행 (앱 실행 시 ~/.claude/settings.json에 훅이 자동 등록됨)
npm start

# 3. Claude CLI 실행
claude
```

## 테스트 방법
1. 터미널에서 `claude` 실행 → 아바타 등장 확인 (SessionStart Hook)
2. 대화 진행 → `Working` 애니메이션 확인 (PreToolUse Hook)
3. 응답 완료 → `Done` 애니메이션 확인 (TaskCompleted Hook)
4. 권한 필요한 작업 요청 → `Help` 상태 확인 (PermissionRequest Hook)
5. 복잡한 태스크 요청 → 서브에이전트 등장 확인 (SubagentStart Hook)
6. 30분 대기 → 자동 제거 확인 (비활성 타임아웃)

## 관련 문서
- **PRD_FINAL.md** - 최종 통합 PRD (완전한 문서)
- **TECHNICAL_PRD.md** - 기술적 관점 통합 PRD
- **PRODUCT_PRD.md** - 제품적 관점 통합 PRD
- **ARCHITECTURE_ANALYSIS.md** - 시스템 아키텍처 분석
- **PRODUCT_ANALYSIS.md** - 제품 기획 분석
- **DEVELOPMENT_ANALYSIS.md** - 개발 품질 분석
- **UI_UX_ANALYSIS.md** - UI/UX 분석
- **QA_ANALYSIS.md** - 품질 보증 분석

---

**버전:** 2.0.0
**최종 업데이트:** 2026-03-05
**문서 상태:** 최종 승인용
